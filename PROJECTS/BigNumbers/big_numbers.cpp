#include<iostream>
#include "numbers.h"
using namespace std;


int main()
{
    num a, b;
    cin >> a;
    cin >> b;
    // a.print();
    num c = (a+b);
    // c.data[0] = 1;
    // cin >> c;
    // c = (a + b);
    cout << (a+b);
    
}
